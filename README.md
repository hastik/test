# Desktop App

This project packages the Primo-generated site as a desktop application using [Electron](https://www.electronjs.org/).

## Prerequisites

- Node.js 18 or later

## Run the app

1. Install dependencies: `npm install`
2. Start the desktop app: `npm start`

Set `ELECTRON_DEVTOOLS=true` before `npm start` if you want the Developer Tools to open automatically.